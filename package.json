{
  "name": "document-analyser",
  "version": "1.0.0",
  "description": "A serverless document analysing system deployed with AWS SAM",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "test": "jest",
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "local": "sam local start-api"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "pdf-parse": "^1.1.1",
    "uuid": "^9.0.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
